webapp/
├─ public/
│  └─ index.php        # Webbappens entrypoint (DocumentRoot)
├─ src/                # (Valfritt) PHP-kod, klasser, helpers
├─ Dockerfile          # Bygger och kör webbappen i container
├─ .dockerignore       # Uteslut vid docker build
├─ .gitignore          # Uteslut i Git
└─ README.md
